// The HIDL LiveDisplay 2.0 interface was removed before LineageOS 23.2.
// Keep the Fujisan implementation source here for its future migration to
// the current AIDL interface, but do not expose the obsolete service.
