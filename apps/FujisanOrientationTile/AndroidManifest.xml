<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zte.fujisan.orientation">

    <!-- Both permissions are signature-level for this platform-signed product app. -->
    <uses-permission android:name="android.permission.SET_ORIENTATION" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_qs_orientation"
        android:allowBackup="false"
        android:theme="@style/AppTheme">
        <service
            android:name=".FujisanOrientationTile"
            android:label="@string/tile_title"
            android:icon="@drawable/ic_qs_orientation"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <activity
            android:name=".OrientationPickerActivity"
            android:label="@string/tile_title"
            android:exported="false"
            android:theme="@style/PickerTheme" />
    </application>
</manifest>
