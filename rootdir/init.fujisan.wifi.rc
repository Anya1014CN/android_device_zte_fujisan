on boot

on post-fs-data
    mkdir /data/vendor/wifi 0770 wifi wifi
    mkdir /data/vendor/wifi/wpa 0770 wifi wifi
    mkdir /data/vendor/wifi/wpa/sockets 0770 wifi wifi
    mkdir /data/vendor/wifi/sockets 0770 wifi wifi
    mkdir /data/vendor/wifi/hostapd 0770 wifi wifi
    mkdir /data/vendor/wifi/hostapd/ctrl 0770 wifi wifi
    mkdir /data/vendor/wifi/wpa_supplicant 0770 wifi wifi
    mkdir /data/misc/wifi 0770 wifi wifi
    mkdir /data/misc/wifi/sockets 0770 wifi wifi
    mkdir /data/misc/wifi/wpa_supplicant 0770 wifi wifi
    copy /vendor/etc/wifi/wpa_supplicant.conf /data/misc/wifi/wpa_supplicant.conf
    copy /vendor/etc/wifi/wpa_supplicant.conf /data/misc/wifi/p2p_supplicant.conf
    chown wifi wifi /data/misc/wifi/wpa_supplicant.conf
    chown wifi wifi /data/misc/wifi/p2p_supplicant.conf
    chmod 0660 /data/misc/wifi/wpa_supplicant.conf
    chmod 0660 /data/misc/wifi/p2p_supplicant.conf

on property:init.svc.vendor.wifi_hal_legacy=running
    # The AIDL Wi-Fi HAL runs as AID_WIFI and switches this legacy qcacld
    # parameter during every Wi-Fi bring-up. The kernel creates it root:root
    # 0644, so hand only this control node to the HAL's service account.
    chown wifi wifi /sys/module/wlan/parameters/fwpath
    chmod 0660 /sys/module/wlan/parameters/fwpath
    write /sys/module/wlan/parameters/fwpath sta
    setprop wlan.driver.status ok

# init.svc.* is a private framework property, so the legacy hostapd cleanup
# trigger must live in system init rather than vendor init.
on property:init.svc.wpa_supplicant=stopped
    stop dhcpcd
    stop hostapd
